## 0.1.1

- Fix: run the script with `with-contenv` so the Supervisor token and add-on environment are available (0.1.0 crash-looped with `SUPERVISOR_TOKEN: unbound variable`).

## 0.1.0

- First release: hourly RTR-Netztest at a configurable minute, four sensors, manual trigger via stdin.
