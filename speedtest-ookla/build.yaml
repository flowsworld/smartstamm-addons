# Built by the Supervisor on the device (no prebuilt image): the Dockerfile only
# adds the run script, the Ookla CLI is downloaded at first start.
build_from:
  aarch64: ghcr.io/home-assistant/aarch64-base:3.22
  amd64: ghcr.io/home-assistant/amd64-base:3.22
